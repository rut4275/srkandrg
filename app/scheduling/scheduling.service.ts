import { HttpClient } from '@angular/common/http';
import { Observable } from 'rxjs';
import { Injectable } from '@angular/core';

@Injectable()
export class StudentService {
    // constructor(private _http: HttpClient) {
    // }

    // getVoluteersByDay(): Observable<classStudent[]> {
    //     return this._http.get<classStudent[]>("/api/Scheduling");
    // }

    // getScheduling(s: classStudent): Observable<classStudent[]> {
    //     return this._http.post<classStudent[]>("/api/Students",s);
    // }

    // saveScheduling(s: classStudent): Observable<classStudent[]> {
    //     return this._http.put<classStudent[]>("/api/Students",s);
    // }
}